{
  "name": "andrewCave",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d"
  },
  "devDependencies": {
    "axios": "^1.11.0",
    "jest": "^30.0.5",
    "jest-environment-node": "^30.0.5"
  }
}
