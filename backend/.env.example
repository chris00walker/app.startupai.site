# ===========================================
# StartupAI Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git (it contains secrets)

# ===========================================
# LLM API KEYS (REQUIRED)
# ===========================================

# OpenAI API Key (REQUIRED for GPT-4 - primary reasoning model)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (optional, for Claude 3 - analytical tasks)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google AI API Key (optional, for Gemini Pro - creative ideation)
GOOGLE_API_KEY=your-google-ai-key-here

# ===========================================
# CREWAI CONFIGURATION
# ===========================================

# Disable telemetry for privacy
CREWAI_TELEMETRY_OPT_OUT=true

# ===========================================
# SUPABASE CONFIGURATION (REQUIRED)
# ===========================================
# Get these from: https://supabase.com/dashboard/project/[YOUR-PROJECT]/settings/api

# Supabase Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Service Role Key (BACKEND ONLY - bypasses RLS)
# ⚠️ CRITICAL: Keep this secret, never expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# DATABASE CONFIGURATION (REQUIRED for Drizzle ORM)
# ===========================================
# Get from: Settings → Database → Connection String (Transaction mode)

# Pooled connection (port 6543) - Use for serverless/Netlify Functions
DATABASE_URL=postgresql://postgres.your-project-ref:your-password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true

# Direct connection (port 5432) - Use for migrations only
DIRECT_DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# ===========================================
# API CONFIGURATION (FastAPI Server - Future)
# ===========================================

API_HOST=0.0.0.0
API_PORT=8000

# ===========================================
# ENVIRONMENT SETTINGS
# ===========================================

# Environment: development | staging | production
ENVIRONMENT=development

# Logging level: debug | info | warning | error
LOG_LEVEL=info

# ===========================================
# NOTES
# ===========================================
# 1. Replace all placeholder values with your actual credentials
# 2. Ensure .env is in .gitignore (already configured)
# 3. For Netlify deployment, set these in Netlify dashboard
# 4. Service role key should ONLY be used in backend/server code
# 5. See SUPABASE_SETUP.md for detailed setup instructions
